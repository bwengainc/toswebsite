         <!-- Main Header -->
    <header class="main-header header-style-two">
    
        <!-- Header Top two -->
     
   <div class="header-top-two">
    <div class="auto-container">
        <div class="inner-container">

            {# Left column with all content centered #}
            <div class="left-column text-center">
                {% if content.bannerImage is defined and content.bannerImage|length > 0 %}
                    {% set banner = content.bannerImage[0] %}
                    {% set image = sulu_resolve_media(banner.id, app.request.locale) %}
                    {% set source = image.formats['sulu-100x100'] is defined and image.formats['sulu-100x100'] is not empty
                        ? image.formats['sulu-100x100']
                        : image.url %}
                    <div class="banner mb-3">
                        <img src="{{ source }}" alt="{{ banner.title|default('Conference Banner') }}">
                    </div>
                {% endif %}

                <div class="text">
                    {#% if content.icon %}
                        <i class="{{ content.icon }}"></i>
                    {% endif %}
                    {% if content.dateText %}
                        <span style="background:#d3d807; font-size:36px; font-weight:bold;">
                            {% if content.dateLink %}
                                <a href="{{ content.dateLink }}">{{ content.dateText }}</a>
                            {% else %}
                                {{ content.dateText }}
                            {% endif %}
                        </span>
                    {% endif %}
                    {% if content.headline %}
                        {{ content.headline }}
                    {% endif %#}
                </div>  
            </div>

            {# Right column stays normal #}
            <div class="right-column">
                {% include '_locale_switcher.html.twig' %}
                {#% if content.ctaButtonLink %}
                    <div class="link-btn">
                        <a href="{{ content.ctaButtonLink }}" class="theme-btn btn-style-one">
                            <span>{{ content.ctaButtonLabel|default('Action') }}</span>
                        </a>
                    </div>
                {% endif %#}
            </div>

        </div>
    </div>
</div>

        <!-- Header Upper -->
        <div class="header-upper">
            <div class="auto-container">
                <div class="inner-container">
                    <!--Logo-->
                    <div class="logo-box">
                        <div class="logo">
     {% set account = webspaceSettings.content.account.entity|default %}
  
   
    {% if account is defined and account.logo is defined %}
    {% set logo = sulu_resolve_media(account.logo.id, app.request.locale) %}
    <a href="{{ sulu_content_root_path() }}" title="{{ account.name }}" rel="home">
        <img src="{{ logo.thumbnails['153x'] }}" 
             alt="{{ logo.title }}" 
             title="{{ logo.description|default(logo.title) }}">
    </a>
{% else %}
    <a href="{{ sulu_content_root_path() }}" rel="home"><img src="/assets/default-logo.png" alt="Default Logo">
    </a>
{% endif %}
                        
    </div>
    </div>
    {#% endif %#}
<!--Nav Box-->
<div class="nav-outer">
<!--Mobile Navigation Toggler-->
<div class="mobile-nav-toggler"><img src="{{ asset('assets/images/icons/icon-bar.png')}}" alt=""></div>
                        

 <!-- Main Menu -->
{% set currentPath = app.request.pathinfo %}
<nav class="main-menu navbar-expand-md navbar-light">
  <div class="collapse navbar-collapse show clearfix" id="navbarSupportedContent">
    <ul class="navigation">
      {% for item in sulu_navigation_root_tree('main', 3) %}
        {% set itemUrl = sulu_content_path(item.url) %}
        {% set hasChildren = item.children|length > 0 %}
        {% set isActive = (currentPath == itemUrl) or (currentPath starts with itemUrl ~ '/') %}

        <li class="{% if hasChildren %}dropdown has-children{% endif %}{% if isActive %} active{% endif %}">
          <a href="{{ itemUrl }}">{{ item.title }}</a>

          {% if hasChildren %}
            <ul>
              {% for child in item.children %}
                {% set childUrl = sulu_content_path(child.url) %}
                {% set childHasChildren = child.children|length > 0 %}
                {% set childActive = (currentPath == childUrl) or (currentPath starts with childUrl ~ '/') %}

                <li class="dropdown{% if childHasChildren %} has-children{% endif %}{% if childActive %} active{% endif %}">
                  <a href="{{ childUrl }}">{{ child.title }}</a>

                  {% if childHasChildren %}
                    <ul>
                      {% for subChild in child.children %}
                        {% set subChildUrl = sulu_content_path(subChild.url) %}
                        {% set subChildActive = (currentPath == subChildUrl) or (currentPath starts with subChildUrl ~ '/') %}

                        <li{% if subChildActive %} class="active"{% endif %}>
                          <a href="{{ subChildUrl }}">{{ subChild.title }}</a>
                        </li>
                      {% endfor %}
                    </ul>
                  {% endif %}
                </li>
              {% endfor %}
            </ul>
          {% endif %}
        </li>
      {% endfor %}
    </ul>
  </div>
</nav>
                    </div>
                    <div class="navbar-right">
                    
                        <!--<div class="search-form-two">
                            <form>
                                <input type="search" placeholder="Explore City ...">
                                <button type="submit"><i class="icon-search"></i></button>
                            </form>
                        </div>-->
                    </div>                       
                </div>
            </div>
        </div>
        <!--End Header Upper-->

        <!-- Sticky Header  -->
        <div class="sticky-header">
            <div class="header-upper">
                <div class="auto-container">
                    <div class="inner-container">
                        <!--Logo-->
                        <div class="logo-box">
                            <div class="logo"><a href="/"><img src="{{ asset('uploads/media/sulu-260x/08/8-eshotlogo.png?v=1-0')}}" alt=""></a></div>
                        </div>
                        <!--Nav Box-->
                        <div class="nav-outer">
                            <!--Mobile Navigation Toggler-->
                            <div class="mobile-nav-toggler"><img src="{{ asset('assets/images/icons/icon-bar.png')}}" alt=""></div>
                            <!-- Main Menu -->
                            <nav class="main-menu navbar-expand-md navbar-light">
                            </nav>
                        </div>
                        <div class="navbar-right">
                            <div class="link-btn"><a href="#" class="theme-btn btn-style-one"><span>Membership</span></a></div>
                        </div>                       
                    </div>
                </div>
            </div>
        </div><!-- End Sticky Menu -->

        <!-- Mobile Menu  -->
        <div class="mobile-menu">
            <div class="menu-backdrop"></div>
            <div class="close-btn"><span class="icon far fa-times-circle"></span></div>
            
            <nav class="menu-box">
                <div class="nav-logo"><a href="index.html"><img src="assets/images/logo.png" alt="" title=""></a></div>
                <div class="menu-outer"><!--Here Menu Will Come Automatically Via Javascript / Same Menu as in Header-->
               
                </div>
				<!--Social Links-->
            </nav>
        </div><!-- End Mobile Menu -->

        <div class="nav-overlay">
            <div class="cursor"></div>
            <div class="cursor-follower"></div>
        </div>
    </header>
  
    <!-- End Main Header -->